package team14.arms.ui.components;

import static org.junit.Assert.assertEquals;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.openqa.selenium.firefox.FirefoxDriver;

import com.vaadin.flow.component.button.testbench.ButtonElement;
import com.vaadin.flow.component.textfield.testbench.TextFieldElement;
import com.vaadin.testbench.TestBenchTestCase;

public class MenuItemCardTest extends TestBenchTestCase {
  
  /*
  @Before
  public void setUp() throws Exception {
    setDriver(new FirefoxDriver());
  }

  /**
   * Opens the menu page
   *
  private void openMenu() {
    getDriver().get("http://localhost:8080/menu");
  }
  
  @Test
  public void testAddItem() throws Exception {
      openMenu();
    
      // Click the button
      ButtonElement button = $(ButtonElement.class).id("add");
      button.click();

      // Check that the label text is correct
      TextFieldElement numberField = $(TextFieldElement.class).first();
      assertEquals("1", numberField.getText());
  }
  
  @After
  public void tearDown() throws Exception {
      driver.quit();
  }*/
}
